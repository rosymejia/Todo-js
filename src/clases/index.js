//va a agrupar todas las class dentro de  la carpeta clases para cuando queramos hacer una 
//importancion no tengmos que importsr ada clase si no q importamos este archivo

import { Todo } from './todo.class';
import { TodoList } from './todo-list-class';

export{
    Todo,
    TodoList
}